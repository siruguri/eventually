<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :required => true, :class => "required" %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :start %><br />
    <%= f.datetime_select :start, minute_step: 15 %>
  </div>
  <div class="field">
    <%= f.label :end %><br />
    <%= f.datetime_select :end, minute_step: 15 %>
  </div>
  <div class="field">
    <%= f.label :in_series %><br />
    <%= f.check_box :in_series %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <div class="fieldinline">
      <%= f.label :vol_need %> <%= f.number_field :vol_need, :required => true %>
    </div>
    <div class="fieldinline">
      <%= f.label :team_name %> <%= f.collection_select :team_id, Team.where(:organization_id => current_user.organization_id), :id, :name %>
    </div>
  </div>

    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
