<%= form_for(@team_role) do |f| %>
  <% if @team_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_role.errors.count, "error") %> prohibited this team_role from being saved:</h2>

      <ul>
      <% @team_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
